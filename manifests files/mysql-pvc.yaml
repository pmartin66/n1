apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc 
  labels: 
     name: mysql-pvc
spec:
  resources:
    requests:
      storage: 2Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
